package utils.common_components.notifications;

import org.openqa.selenium.WebDriver;
import utils.selenium_utils.SeleniumUtils;

public class Notifications extends SeleniumUtils {
    public Notifications(WebDriver driver) {
        super(driver);
    }

}
